package com.vibeapp.ui.theme

import androidx.compose.ui.graphics.Color

// Deep dark background
val Background = Color(0xFF0D0D12)
val Surface = Color(0xFF16161F)
val SurfaceVariant = Color(0xFF1E1E2A)
val SurfaceElevated = Color(0xFF252532)

// Accent — electric violet-blue
val Primary = Color(0xFF7C5CFC)
val PrimaryVariant = Color(0xFF9B7EFF)
val PrimaryDim = Color(0xFF3D2E80)

// Status colors
val ColorConnected = Color(0xFF4ADE80)     // Vivid green
val ColorConnecting = Color(0xFFFBBF24)    // Amber
val ColorDisconnected = Color(0xFFEF4444) // Red
val ColorReconnecting = Color(0xFF60A5FA) // Blue

// Delivery status
val ColorDelivered = Color(0xFF34D399)     // Teal green
val ColorVibrationStarted = Color(0xFF4ADE80)
val ColorFailed = Color(0xFFEF4444)
val ColorTimeout = Color(0xFFFF6B35)

// Text
val TextPrimary = Color(0xFFF0F0FF)
val TextSecondary = Color(0xFF9090B0)
val TextTertiary = Color(0xFF606075)

// Button colors
val ButtonPrimary = Color(0xFF7C5CFC)
val ButtonManualActive = Color(0xFF4ADE80)
val ButtonSide = Color(0xFF252532)

// Borders
val BorderSubtle = Color(0xFF2A2A3A)
val BorderActive = Color(0xFF7C5CFC)
